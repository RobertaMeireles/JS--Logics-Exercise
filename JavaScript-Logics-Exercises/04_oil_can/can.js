/*
Calculate and display the volume value of a
can of oil, using the formula VOLUME <- 3.14159 * R ^ 2 * HEIGHT
*/

let raio = 1;
let altura = 3;
let volume;


function calculo (raio,altura) {
    return volume= (3,14159 * ((raio*raio)*altura));
}

console.log(calculo(raio,altura));

//formula volume= (3,14159 * Raio^2*altura);